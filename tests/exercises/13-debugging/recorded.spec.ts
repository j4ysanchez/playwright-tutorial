import { test, expect } from "@playwright/test";

// TODO (Exercise 2): replace this with your own codegen output.
// Run: npx playwright codegen localhost:5173
// Record: add two different pizzas (different sizes) to the cart, then land on /cart.
// Clean up the generated locators to use role/label-based queries, and add a real
// assertion at the end (e.g. expect(page.getByTestId("cart-item")).toHaveCount(2)).

test.fixme("two different pizzas end up in the cart", async ({ page }) => {
  // paste and clean up your recorded steps here
});
