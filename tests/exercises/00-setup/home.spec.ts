import { test, expect } from "@playwright/test";

test("home page loads and shows the menu heading", async ({ page }) => {
  // Remove this line once you start implementing the test below.
  // test.fixme() marks the test as "not done yet" instead of silently
  // passing an empty test body.
  test.fixme();

  // TODO 1: navigate to "/"
  // TODO 2: assert the document title contains "Slice House"
  //         hint: expect(page).toHaveTitle(/.../)
  // TODO 3: assert a heading named "Our Menu" is visible
  //         hint: page.getByRole("heading", { name: "..." })
});
