import { test, expect } from "@playwright/test";

// TODO: define the cases array described in the README, e.g.:
// const cases: { size: "S" | "M" | "L"; toppings: string[]; expectedTotal: string }[] = [
//   { size: "S", toppings: [], expectedTotal: "8.99" },
//   ...
// ];

// TODO: loop over `cases` here, calling `test(...)` once per case.
// Each test body should:
//   1. page.goto("/pizza/margherita")
//   2. select the size radio for `size`
//   3. check each topping in `toppings`
//   4. assert page.getByTestId("unit-price") contains `expectedTotal`
// Wrap each of steps 2-4 in test.step() for a readable report.

test.fixme("remove this placeholder once you've added the generated tests above", () => {});
